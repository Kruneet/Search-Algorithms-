The Java code uses BFS search technique to search through expanded nodes. 
Also the input initial state is suppose to be given as a hexadecimal string(0-F), where 0 represents the blank square.
No special instructions on executing is required, just compile and execute it.